import 'package:flutter/material.dart';

class EventsEvent extends StatelessWidget {
  const EventsEvent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Event'),
        centerTitle: true,
      ),
    );
  }
}